<?xml version="1.0" encoding="UTF-8"?><extensionAttribute>	<displayName>Battery Charge Percentage</displayName>	<displayInCategory>System Information</displayInCategory>	<dataType>integer</dataType>	<description>This attribute returns remaining charge percentage of the battery, if installed.  This attribute applies to both Mac and Windows.</description>	<scriptContentsMac>#!/bin/shmaxCapacity=`ioreg -r -c "AppleSmartBattery" | grep -w "MaxCapacity" | awk '{print $3}' | sed s/\"//g`currentCapacity=`ioreg -r -c "AppleSmartBattery" | grep -w "CurrentCapacity" | awk '{print $3}' | sed s/\"//g`	result=$( echo "scale=4; ($currentCapacity / $maxCapacity) * 100" | bc | sed 's/\(.*\)../\1/' )echo "&lt;result&gt;$result&lt;/result&gt;"	</scriptContentsMac>	<scriptTypeWindows>VBScript</scriptTypeWindows>	<scriptContentsWindows>On Error Resume Next	Dim objBatteriesDim strBatteries	Set objBatteries = GetObject("winmgmts:").InstancesOf("Win32_Battery")i=0For each Battery in objBatteriesif i &lt;&gt; 0 thenstrBatteries = "&lt;br&gt;" &amp; strBatteriesEnd IfstrBatteries = Battery.EstimatedChargeRemaining &amp; strBatteries    i=i+1NextWScript.Echo "&lt;result&gt;" &amp; strBatteries &amp; "&lt;/result&gt;"</scriptContentsWindows></extensionAttribute>