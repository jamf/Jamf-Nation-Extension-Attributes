<?xml version="1.0" encoding="UTF-8"?><extensionAttribute>	<displayName>OS - Firewall Timeout</displayName>	<displayInCategory>Security Reporting</displayInCategory>	<dataType>string</dataType>	<description>This extension attribute will verify that the firewall timeout is set to 3 seconds rather than the 5 minute default.</description>	<scriptContentsMac>#!/bin/shdesiredResult="3"if [ -f /usr/libexec/PlistBuddy ]; then result=`/usr/libexec/PlistBuddy -c "print rights:com.alf:timeout" /private/etc/authorization` if [ "$result" == "$desiredResult" ]; then	echo "&lt;result&gt;Pass ($result)&lt;/result&gt;"else	echo "&lt;result&gt;Fail ($result)&lt;/result&gt;"fielseecho "&lt;result&gt;Please ensure that PlistBuddy is installed at /usr/libexec/PlistBuddy.&lt;/result&gt;"fi	</scriptContentsMac></extensionAttribute>